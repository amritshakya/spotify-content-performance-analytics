# Minimal dependency set for the data-foundation / SQL-analytics phase.
# Intentionally excludes scipy, statsmodels, scikit-learn, and any
# dashboard/visualization framework beyond matplotlib -- those are
# reserved for a later phase (see README "Planned Modeling").

pandas==2.3.3
numpy>=1.26,<3
duckdb>=1.0,<2
pyarrow>=16.0
matplotlib>=3.8
jupyter>=1.0
pytest>=8.0
