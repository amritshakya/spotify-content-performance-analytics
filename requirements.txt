# Phase 2: data-foundation / SQL-analytics dependencies.
pandas==2.3.3
numpy>=1.26,<3
duckdb>=1.0,<2
pyarrow>=16.0
matplotlib>=3.8
jupyter>=1.0
pytest>=8.0

# Phase 3: interpretable modeling layer. No dashboard/deployment
# frameworks, and no tree-ensemble/boosting/neural-network libraries --
# see README "Scope Limit".
scikit-learn>=1.4
statsmodels>=0.14
